import { ChatLayout } from '../components/chat';

const Chat = () => {
  return (
    <div>
      <h1 className="text-2xl font-semibold text-gray-900 mb-6">チャット</h1>
      <ChatLayout />
    </div>
  );
};

export default Chat;
